camel: 1.0.0
name: default/ident_bounding_boxes
title:  ident_bounding_boxes Skill
description: Update this description --  ident_bounding_boxes
inputs:
  - name: input1
    title: input1
    parameters:
      - name: text
        type: string
        description: Update this description
        required: true
    routing:
      all:
        action  : default/ident_bounding_boxes
        runtime: cortex/functions
        output: output1

outputs:
  - name: output1
    title: output1
    parameters:
      - name: text
        type: string
        description: Update this description
        required: true
